name = "ab2pro-outils"
main = "src/worker.js"
compatibility_date = "2026-08-01"

# Fichiers statiques (connexion, admin, applications) â€” servis UNIQUEMENT via le Worker (contrÃ´le de session)
[assets]
directory = "assets"
binding = "ASSETS"
run_worker_first = true
html_handling = "none"        # chemins servis tels quels : le Worker fait son propre routage
not_found_handling = "none"

[[d1_databases]]
binding = "DB"
database_name = "ab2pro-auth"
database_id = "5ced4603-9318-4f6a-b372-85da4f2fa910"   # <- coller l'id renvoyÃ© par : wrangler d1 create ab2pro-auth

# Secrets (Ã  dÃ©finir via `wrangler secret put`, JAMAIS en clair ici) :
#   RESEND_API_KEY  â€” clÃ© API Resend (e-mails demandes d'accÃ¨s / invitations)
# Variable optionnelle :
[vars]
EMAIL_FROM = "AB2Pro <onboarding@resend.dev>"   # aprÃ¨s vÃ©rification d'un domaine chez Resend : "AB2Pro <acces@votredomaine.fr>"


